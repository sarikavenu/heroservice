server:
  port: 9000
spring:
  application:
    name: heroservice
eureka:
  instance:
    hostname:heroservice
  client:
    service-url:
      defaultZone: http://eureka-app1:8761/eureka/
management:
  endpoints:
    web:
      exposure:
        include: '*'
